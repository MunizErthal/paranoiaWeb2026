import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-loja',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './loja.html',
  styleUrl: './loja.scss'
})
export class Loja {}
